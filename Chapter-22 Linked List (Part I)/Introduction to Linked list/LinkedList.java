import java.util.*;
public class LinkedList{
  public static void main(String args[]){

  // Linked ListsLinked Lists

  // A linked list is a linear data structure where elements (called nodes) are stored in sequence, but unlike arrays, they aren't stored in contiguous memory locations. Instead, each node holds data and a pointer to the next node.
  
  // Structure of a Node
  // [ Data | Next ] --> [ Data | Next ] --> [ Data | Next ] --> NULL
  
  // Each node contains:
  // Data — the value stored
  // Next — a pointer/reference to the next node
  }
}

//Introduction to Linked list
